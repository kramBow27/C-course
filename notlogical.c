#include <stdio.h>;
#include <stdbool.h>;

int main() {

    bool sunny =true;

    if (!sunny) {
        printf("\nIt's sunny outside!");
    } else{
        printf("\nIt's cloudy outtside!");
    } 
    return 0;
}